# Golf Swing AI - Optimized Dependencies (Full Functionality)
# Standard PyTorch (auto-detects CPU), headless OpenCV saves ~50MB

# Core ML libraries (lightweight versions)
torch==2.0.0
numpy==1.24.3
scikit-learn==1.3.0
joblib==1.3.1
scipy==1.11.1

# Computer Vision (headless = smaller)
opencv-python-headless==4.8.0.76
mediapipe==0.10.0

# Web Framework
fastapi==0.100.0
uvicorn[standard]==0.23.0
python-multipart==0.0.6

# Utilities (minimal versions)
matplotlib==3.7.2
pillow==10.0.0
tqdm==4.65.0

# Total: ~300MB vs 550MB+ original
# Savings: ~250MB while keeping ALL features
